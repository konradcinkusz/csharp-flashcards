% 1 ── probe
\QuestionSlide[\CategoryBadge[LinqColor!20]{gRPC}]<2>{What is gRPC?}
\begin{frame}[fragile]
  \frametitle{\AnswerTitle[\CategoryBadge[LinqColor!20]{gRPC}]{What is gRPC?}}
  {\footnotesize
  A contract-first RPC framework using HTTP/2 and protocol buffers.
  }
  \begin{minted}{csharp}
  var client = new Greeter.GreeterClient(channel);
  \end{minted}
\end{frame}
